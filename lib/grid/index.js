(function (w) {

    EASEL_MAP.Grid = function (map, params) {
        this.map = map;
        _.extend(this, params);

    };

    EASEL_MAP.Grid.prototype = {
        render: function(location, filter){

        }
    };


})(window)